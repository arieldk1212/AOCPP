#ifndef AOCPP_DAY_2_H
#define AOCPP_DAY_2_H

#include <fstream>
#include <string>

// we need to iterate each side, and check how many times a number appears
// on the different side, and multiply that number by the number of
// apperences, add it to the total score.

bool aoc_2024_day2(std::ifstream &data) {
  int sim_score = 0;
  std::string Stream;

  while (std::getline(data, Stream)) {
    char Delimitor = ' ';
  }

  return true;
}

#endif